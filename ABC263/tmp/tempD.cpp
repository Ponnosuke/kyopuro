#include <bits/stdc++.h>
using namespace std;

using i64 = int64_t;
using ui64 = uint64_t;

int main() {
    i64 N, L, R;
    cin >> N >> L >> R;
    vector<i64> A(N + 1);
    for(int i = 1; i <= N; i++) cin >> A.at(i);

    vector<pair<i64, i64>> x(N + 1), y(N + 1);  // 置き換え数 加算数

    
}